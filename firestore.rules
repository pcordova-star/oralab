rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS TOTALMENTE ABIERTAS (EMERGENCIA)
    // Forzando actualización para propagación: 2024-05-24T22:45:00Z
    match /{document=**} {
      allow read, write: if true;
    }
  }
}