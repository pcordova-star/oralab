
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // DESACTIVADO TEMPORALMENTE: Permite acceso total para desarrollo
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
