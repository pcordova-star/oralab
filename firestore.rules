
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // APERTURA TOTAL PARA DESARROLLO
    // Permite que cualquier operación de lectura o escritura sea exitosa sin importar el usuario.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
