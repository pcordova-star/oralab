rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // ACCESO TOTAL PARA DESARROLLO Y CAPTACIÓN PÚBLICA
    // Simplificado al máximo para evitar errores de permisos
    match /{document=**} {
      allow read, write: if true;
    }
  }
}