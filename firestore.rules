rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS DE APERTURA TOTAL - FORZANDO ACTUALIZACIÓN
    // Timestamp de despliegue: 2026-05-24T20:15:00Z
    
    match /{document=**} {
      allow read, write: if true;
    }
  }
}