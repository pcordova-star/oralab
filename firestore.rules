rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS TEMPORALES PARA DESARROLLO Y PRUEBAS
    // Permite lectura y escritura a cualquier usuario para depurar el error de "insufficient permissions"
    match /{document=**} {
      allow read, write: if true;
    }
  }
}