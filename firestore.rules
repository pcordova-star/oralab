rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // REGLAS ABIERTAS PARA DESARROLLO Y DEPURACIÓN
    // Permite lectura y escritura a cualquier usuario sin restricciones.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}